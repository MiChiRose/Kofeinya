import React from "react";
import Container from "../../reusableComponents/Container";

interface Props {
    navigation: any
}

export const FavoritesScreen = ({ navigation }: Props): JSX.Element => {
    return (
        <Container isHeaderHidden navigation={navigation}>

        </Container>
    )
}